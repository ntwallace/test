import { Convert24To12FormatPipe } from './convert-24-to-12-format.pipe';

describe('Convert24To12FormatPipe', () => {
    it('create an instance', () => {
        const pipe = new Convert24To12FormatPipe();
        expect(pipe).toBeTruthy();
    });
});
